{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Portfolio website to showcase my projects and my resume.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist/ && babel ./ --out-dir dist/ --ignore ./node_modules,./.babelrc,./package.json,./npm-debug.log,./.jshintrc,./yarn.lock --copy-files",
    "start": "npm run build && node dist/index.js"
  },
  "author": "Sam Galizia",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.2"
  },
  "devDependencies": {
    "@babel/core": "^7.0.0-beta.31",
    "@babel/preset-env": "^7.0.0-beta.31",
    "babel-cli": "^6.26.0",
    "rimraf": "^2.6.2"
  }
}
